import { CreateCollectionDto } from './create-collection.dto';

describe('CreateCollectionDto', () => {
  it('should be defined', () => {
    expect(new CreateCollectionDto()).toBeDefined();
  });
});
