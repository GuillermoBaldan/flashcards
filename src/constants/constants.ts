export const ALLOWED_GAME_MODES = ['be-honest', 'riddle', 'true-or-false'];
export const DIFFICULTY_LEVELS = ['calibrate', 'easy', 'medium', 'hard'];
export const ALLOWED_CARD_COUNTS = [10, 15, 20];
export type StatusType = 'pending' | 'accepted' | 'rejected' | 'expired';
export type ResponseType = 'accepted' | 'rejected';
