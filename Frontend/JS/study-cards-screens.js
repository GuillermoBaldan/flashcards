//Information Pannel
writeCollectionName();
writeFlashcardsToSee(dataDecks,valorDeck); //writes the number of cards for reviewing
writeReviewedCards(queue)//write the numbers of cards reviewed

//First, we star with the question screen
questionScreen();